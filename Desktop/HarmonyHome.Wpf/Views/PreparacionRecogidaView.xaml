<Window x:Class="HarmonyHome.Wpf.Views.PreparacionRecogidaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarmonyHome.Wpf.Views"
        mc:Ignorable="d"
        Title="PreparacionRecogidaView" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Preparación de recogida"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBlock x:Name="TxtInfo"
                   Grid.Row="1"
                   Text=""
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <DataGrid x:Name="TablaPreparacion"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding ProductoNombre}" Width="180"/>
                <DataGridTextColumn Header="Solicitada" Binding="{Binding CantidadSolicitada}" Width="90"/>
                <DataGridTextColumn Header="Ubicación" Binding="{Binding UbicacionNombre}" Width="220"/>
                <DataGridTextColumn Header="Código" Binding="{Binding UbicacionCodigo}" Width="130"/>
                <DataGridTextColumn Header="Disponible" Binding="{Binding CantidadDisponible}" Width="90"/>
                <DataGridTextColumn Header="A recoger" Binding="{Binding CantidadARecoger}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>