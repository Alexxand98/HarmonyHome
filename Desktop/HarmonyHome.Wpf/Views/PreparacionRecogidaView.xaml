<Window x:Class="HarmonyHome.Wpf.Views.PreparacionRecogidaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarmonyHome.Wpf.Views"
        mc:Ignorable="d"
        Title="Preparación de recogida"
        Height="560"
        Width="950"
        MinHeight="520"
        MinWidth="850"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ColorFondoApp}">

    <Grid Margin="22"
          Background="{StaticResource ColorFondoApp}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource ColorVerdePanel}"
                BorderBrush="{StaticResource ColorBorde}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">

            <StackPanel>
                <TextBlock Text="Preparación de recogida"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="{StaticResource ColorTextoPrincipal}"/>

                <TextBlock Text="Consulta las ubicaciones y cantidades necesarias para preparar la orden seleccionada."
                           FontSize="14"
                           Foreground="{StaticResource ColorTextoSecundario}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Style="{StaticResource CardPanel}"
                Margin="0,0,0,14">

            <TextBlock x:Name="TxtInfo"
                       Text=""
                       FontSize="14"
                       Foreground="{StaticResource ColorTextoPrincipal}"
                       TextWrapping="Wrap"/>
        </Border>

        <Border Grid.Row="2"
                Style="{StaticResource CardPanel}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Líneas de preparación"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource ColorTextoPrincipal}"
                           Margin="0,0,0,12"/>

                <DataGrid x:Name="TablaPreparacion"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Producto"
                                            Binding="{Binding ProductoNombre}"
                                            Width="*"/>

                        <DataGridTextColumn Header="Solicitada"
                                            Binding="{Binding CantidadSolicitada}"
                                            Width="110"/>

                        <DataGridTextColumn Header="Ubicación"
                                            Binding="{Binding UbicacionNombre}"
                                            Width="220"/>

                        <DataGridTextColumn Header="Código"
                                            Binding="{Binding UbicacionCodigo}"
                                            Width="130"/>

                        <DataGridTextColumn Header="Disponible"
                                            Binding="{Binding CantidadDisponible}"
                                            Width="110"/>

                        <DataGridTextColumn Header="A recoger"
                                            Binding="{Binding CantidadARecoger}"
                                            Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>