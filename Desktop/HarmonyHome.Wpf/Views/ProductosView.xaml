<Window x:Class="HarmonyHome.Wpf.Views.ProductosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Productos"
        Height="650"
        Width="1150"
        MinHeight="580"
        MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ColorFondoApp}">

    <Grid Margin="22"
          Background="{StaticResource ColorFondoApp}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource ColorVerdePanel}"
                BorderBrush="{StaticResource ColorBorde}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">

            <StackPanel>
                <TextBlock Text="Productos"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="{StaticResource ColorTextoPrincipal}"/>

                <TextBlock Text="Consulta el catálogo de productos registrados en HarmonyHome."
                           FontSize="14"
                           Foreground="{StaticResource ColorTextoSecundario}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Style="{StaticResource CardPanel}"
                Margin="0,0,0,14">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Filtros y acciones"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{StaticResource ColorTextoPrincipal}"
                           Margin="0,0,0,12"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="BtnCargarProductos"
                            Grid.Column="0"
                            Content="Cargar productos"
                            Width="145"
                            Click="BtnCargarProductos_Click"/>

                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right">

                        <TextBlock Text="Buscar"
                                   FontSize="13"
                                   Foreground="{StaticResource ColorTextoSecundario}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>

                        <TextBox x:Name="TxtBuscarProducto"
                                 Width="250"
                                 Margin="0,0,8,0"/>

                        <Button x:Name="BtnBuscarProducto"
                                Content="Buscar"
                                Width="85"
                                Click="BtnBuscarProducto_Click"/>

                        <Button x:Name="BtnLimpiarBusquedaProducto"
                                Content="Limpiar"
                                Width="85"
                                Style="{StaticResource BotonPeligro}"
                                Click="BtnLimpiarBusquedaProducto_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Style="{StaticResource CardPanel}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Listado de productos"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource ColorTextoPrincipal}"
                           Margin="0,0,0,12"/>

                <DataGrid x:Name="TablaProductos"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Referencia"
                                            Binding="{Binding Referencia}"
                                            Width="130"/>

                        <DataGridTextColumn Header="Nombre"
                                            Binding="{Binding Nombre}"
                                            Width="*"/>

                        <DataGridTextColumn Header="Categoría"
                                            Binding="{Binding Categoria}"
                                            Width="150"/>

                        <DataGridTextColumn Header="Trazabilidad"
                                            Binding="{Binding TipoTrazabilidadNombre}"
                                            Width="150"/>

                        <DataGridTextColumn Header="Precio"
                                            Binding="{Binding PrecioVenta}"
                                            Width="100"/>

                        <DataGridCheckBoxColumn Header="Habilitado"
                                                Binding="{Binding Habilitado}"
                                                Width="110"/>

                        <DataGridCheckBoxColumn Header="Activo"
                                                Binding="{Binding Activo}"
                                                Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <TextBlock x:Name="TxtMensajeProductos"
                   Grid.Row="3"
                   Text=""
                   Style="{StaticResource TextoMensaje}"/>
    </Grid>
</Window>